{
  "name": "coa-transfer",
  "version": "1.0.0",
  "dependencies": {
    "playwright": "^1.40.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "scripts": {
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "engines": {
    "node": ">=18"
  }
}
